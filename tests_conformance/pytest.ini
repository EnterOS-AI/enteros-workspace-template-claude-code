[pytest]
# Anchor rootdir here (like tests/pytest.ini) so pytest never treats the
# template dir's __init__.py as a package node (its relative `from .adapter
# import ...` needs the runtime deps). importlib import-mode + auto asyncio
# mirror the unit suite so the inherited async conformance tests run.
addopts = --import-mode=importlib
asyncio_mode = auto
